# Snake_Game_Using_Hand_Tracking
This code creates a gesture-controlled Nokia Snake game using OpenCV, Pygame, and threading. It captures webcam input, detects hand gestures to control snake movement and speed boost, runs gesture detection in a separate thread, updates the game in real time, handles restart and exit events, and properly cleans up resources when the game closes.
